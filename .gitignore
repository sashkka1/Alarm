*.iml
.gradle/
.idea/
build/
local.properties
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab
builds/

# Личные данные: журнал событий, сон, подъёмы. Лежат в папке проекта по решению
# владельца, но в git не попадают никогда.
data/
*.csv

# Нативный движок распознавания речи и модели к нему: ~90 МБ библиотек и сотни
# мегабайт весов. Кладутся `npm run stt:setup`, в git не попадают никогда.
runtime/

# Кэш компилятора Kotlin.
.kotlin/

# Видео и прочие тяжёлые записи с телефона: разбор по ним уже записан в устав,
# а сами файлы весят десятки мегабайт и репозиторий публичный.
*.mp4

# ⚠️ Документы работы в git не идут (решение владельца 2026-08-29): ни доски, ни
# ADR, ни карты работ, ни сам устав. Репозиторий несёт только код.
docs/
CLAUDE.md

